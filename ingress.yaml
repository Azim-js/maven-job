apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hello
  namespace: example
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/subnets: subnet-09d327eced3596056
    alb.ingress.kubernetes.io/scheme: internet-facing
spec:
  ingressClassName: alb  # Replace kubernetes.io/ingress.class with spec.ingressClassName
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: my-app-chart
            port:
              number: 80
